import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    height: 52,
    maxHeight: 52,
  },
  ingredientsContent: {
    gap: 12,
    paddingHorizontal: 22,
  },
})
